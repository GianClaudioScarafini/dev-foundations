// CLOSURES
// A closure is a function that "remembers" variables from its outer scope
// even after that outer function has finished executing.

// ─────────────────────────────────────────────
// 1. Make a counter
//    Each call to counter() should increment and return the count
//    Expected:
//      counter() → 1
//      counter() → 2
//      counter() → 3

// your code here

// ─────────────────────────────────────────────
// 2. Make a multiplier factory
//    multiplyBy(3) returns a function that multiplies any number by 3
//    Expected:
//      const triple = multiplyBy(3)
//      triple(5) → 15
//      triple(10) → 30

// your code here

// ─────────────────────────────────────────────
// 3. Make a greeting factory
//    greet("Hello") returns a function that greets any name
//    Expected:
//      const sayHello = greet("Hello")
//      sayHello("Alice") → "Hello, Alice!"
//      sayHello("Bob")   → "Hello, Bob!"

// your code here

// ─────────────────────────────────────────────
// 4. Make a once() function
//    once(fn) returns a new function that only calls fn the first time
//    After that it returns the first result, no matter how many times called
//    Expected:
//      const init = once(() => "initialised!")
//      init() → "initialised!"
//      init() → "initialised!"  ← same result, fn not called again

// your code here
