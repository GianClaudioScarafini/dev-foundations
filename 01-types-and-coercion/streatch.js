// What does this output? Work it out before running it.
console.log([] == false) // this should be true becusae the list if does not have a value
console.log([] === false) // this should be false because the the type are different 
console.log([] + []) // should return ""
console.log([] + {}) // the tyep are different i guess should be an object
console.log({} + [])// the tyep are different i guess should be an object
console.log(""==0)