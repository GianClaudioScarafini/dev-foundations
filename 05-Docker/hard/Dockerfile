# TODO: Write a multi-stage Dockerfile
#
# Stage 1 (builder): install all dependencies
# Stage 2 (production): copy only what's needed to run
#
# Key question: what goes in stage 1 but NOT stage 2?
# Why does this make the final image smaller?
#
# Hint: two FROM instructions, use AS to name them
# Hint: COPY --from=builder copies files between stages
